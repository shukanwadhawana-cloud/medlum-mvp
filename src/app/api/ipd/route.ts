import { NextResponse } from "next/server";
import { prisma } from "@/lib/db";
import { getSession } from "@/lib/session";
import { writeAudit } from "@/lib/audit";
import { cleanPatientNotes, encodePatientNotes, parseCareSetting, parsePatientProfile } from "@/lib/patient-metadata";
import { requireClinicalModule } from "@/lib/clinic-products";
import { requireActiveClinicMembership, normalizeClinicRole } from "@/lib/clinic-auth";
import { istIsoLabel } from "@/lib/time";
import { hashClinicalNote } from "@/lib/clinical-signing";

async function getClinicId(doctorId:string){return (await prisma.clinicMember.findFirst({where:{doctorId,isActive:true},select:{clinicId:true}}))?.clinicId||null;}
function metaOf(log:any){try{return typeof log.meta==="string"?JSON.parse(log.meta||"{}"):log.meta||{}}catch{return{}}}
function abnormalVitals(v:any){const n=(x:any)=>parseFloat(String(x));const spo=n(v.spo2);return (Number.isFinite(spo)&&spo<94)||(Number.isFinite(n(v.pulse))&&(n(v.pulse)<50||n(v.pulse)>120))||(Number.isFinite(n(v.rr))&&(n(v.rr)<10||n(v.rr)>30))||(Number.isFinite(n(v.temperature))&&(n(v.temperature)<35||n(v.temperature)>38.5))||(String(v.bp||"").includes("/")&&n(String(v.bp).split("/")[0])<90);}
async function getPatient(patientId:string,doctorId:string,clinicId:string|null){return prisma.patient.findFirst({where:clinicId?{id:patientId,clinicId}:{id:patientId,doctorId}});}
export async function GET(){const session=await getSession();if(!session)return NextResponse.json({error:"Unauthorized"},{status:401});const access=await requireClinicalModule(session.doctorId,"IPD");if(!access.allowed)return NextResponse.json({error:"IPD access is not included in this clinic's subscription."},{status:403});const clinicId=access.clinicId;const doctorIds=clinicId?(await prisma.clinicMember.findMany({where:{clinicId,isActive:true},select:{doctorId:true}})).map(x=>x.doctorId):[session.doctorId];const patients=await prisma.patient.findMany({where:clinicId?{clinicId,doctorId:{in:doctorIds}}:{doctorId:{in:doctorIds}},orderBy:{createdAt:"desc"}});const patientIds=patients.map(p=>p.id),patientIdSet=new Set(patientIds);const doctorRows=await prisma.doctor.findMany({where:{id:{in:doctorIds}},select:{id:true,name:true}});const doctorNameById=new Map(doctorRows.map(d=>[d.id,d.name]));const [logs,encounters,prescriptions,dispensings,labOrders,diagnosticOrders,clinicalNotes,handoverLogs]=await Promise.all([prisma.auditLog.findMany({where:{doctorId:{in:doctorIds},entity:{in:["HospitalRoom","ClinicalNote","NursingVital"]}},orderBy:{createdAt:"desc"},take:3000}),prisma.encounter.findMany({where:{patientId:{in:patientIds}},orderBy:{createdAt:"desc"}}),prisma.prescription.findMany({where:{patientId:{in:patientIds}},orderBy:{createdAt:"desc"}}),prisma.dispensing.findMany({where:{patientId:{in:patientIds}},orderBy:{createdAt:"desc"}}),prisma.labOrder.findMany({where:{patientId:{in:patientIds}},orderBy:{createdAt:"desc"}}),prisma.diagnosticOrder.findMany({where:{patientId:{in:patientIds}},orderBy:{createdAt:"desc"}}),prisma.clinicalNote.findMany({where:{patientId:{in:patientIds},clinicId:clinicId||undefined},include:{author:{select:{id:true,name:true,email:true}},verifier:{select:{id:true,name:true,email:true}}},orderBy:{createdAt:"desc"}}),prisma.auditLog.findMany({where:{doctorId:{in:doctorIds},entity:"IPDHandover",entityId:{in:patientIds}},orderBy:{createdAt:"desc"},take:3000})]);const roomsMap=new Map<string,any>(),notesMap=new Map<string,any[]>(),vitalsMap=new Map<string,any[]>(),handoverMap=new Map<string,any>(),dispensingByPrescriptionId=new Map(dispensings.map(d=>[d.prescriptionId,d]));for(const l of logs){const m=metaOf(l);if(l.entity==="HospitalRoom"&&m.roomNumber&&(!clinicId||m.clinicId===clinicId)&&!roomsMap.has(String(m.roomNumber)))roomsMap.set(String(m.roomNumber),{id:l.entityId||String(m.roomNumber),...m});if(l.entity==="ClinicalNote"&&l.entityId&&patientIdSet.has(l.entityId)){const a=notesMap.get(l.entityId)||[];a.push({id:l.id,...m,authorName:doctorNameById.get(l.doctorId??"")||"Clinician",createdAt:l.createdAt.toISOString()});notesMap.set(l.entityId,a)}if(l.entity==="NursingVital"&&l.entityId&&patientIdSet.has(l.entityId)){const a=vitalsMap.get(l.entityId)||[];a.push({id:l.id,...m,createdAt:l.createdAt.toISOString(),authorName:doctorNameById.get(l.doctorId??"")||"Clinician"});vitalsMap.set(l.entityId,a)}}for(const n of clinicalNotes){const a=notesMap.get(n.patientId)||[];a.push({id:n.id,noteType:n.noteType,title:n.title,content:n.content,status:n.status,authorRole:"Clinical",authorName:n.author?.name||"Clinician",authorDoctorId:n.authorDoctorId,verifierName:n.verifier?.name||null,verifierDoctorId:n.verifierDoctorId||null,finalizedAt:n.finalizedAt?.toISOString()||null,createdAt:n.createdAt.toISOString(),contentHash:n.contentHash,finalHash:n.finalHash});notesMap.set(n.patientId,a)}for(const l of handoverLogs){if(l.entityId&&patientIdSet.has(l.entityId)&&!handoverMap.has(l.entityId)){const m=metaOf(l);handoverMap.set(l.entityId,{id:l.id,...m,createdAt:l.createdAt.toISOString(),actorName:m.actorName||doctorNameById.get(l.doctorId??"")||"Clinician"});}}for(const e of encounters){const a=notesMap.get(e.patientId)||[];const m=String(e.clinicalNotes||"");if(m){const match=m.match(/^\[([^\]]+)\]\s*\n?([\s\S]*)$/);a.push({id:e.id,noteType:match?.[1]||"Consultant Note",content:match?.[2]||m,authorRole:(match?.[1]||"Consultant Note").split(" ")[0],status:"Open",authorName:doctorNameById.get(e.doctorId??"")||"Clinician",createdAt:e.createdAt.toISOString()});notesMap.set(e.patientId,a)}if(e.bp||e.pulse||e.rr||e.spo2||e.temperature||e.weight||e.height){const v=vitalsMap.get(e.patientId)||[];v.push({id:e.id,bp:e.bp,pulse:e.pulse,rr:e.rr,spo2:e.spo2,temperature:e.temperature,weight:e.weight,height:e.height,createdAt:e.createdAt.toISOString(),authorName:doctorNameById.get(e.doctorId??"")||"Clinician",source:"OPD"});v.sort((x,y)=>new Date(y.createdAt).getTime()-new Date(x.createdAt).getTime());vitalsMap.set(e.patientId,v)}}for(const r of prescriptions){if(!r.medicines)continue;const a=notesMap.get(r.patientId)||[];a.push({id:r.id,noteType:"Medication Indent",content:r.medicines,authorRole:"Medication",status:"Pending",authorName:doctorNameById.get(r.doctorId??"")||"Clinician",createdAt:r.createdAt.toISOString(),prescriptionId:r.id,pharmacyStatus:dispensingByPrescriptionId.get(r.id)?.status||"Not sent to pharmacy",dispensingId:dispensingByPrescriptionId.get(r.id)?.id||null,dispensedAt:dispensingByPrescriptionId.get(r.id)?.dispensedAt?.toISOString()||null});notesMap.set(r.patientId,a)}const ipd=patients.filter(p=>parseCareSetting(p.notes)==="IPD"&&p.status==="ACTIVE").map(p=>{const profile=parsePatientProfile(p.notes),vs=vitalsMap.get(p.id)||[],latest=vs[0],notes=(notesMap.get(p.id)||[]).sort((a,b)=>new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime()),orders=labOrders.filter(l=>l.patientId===p.id).sort((a,b)=>new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime());return{id:p.id,name:p.name,age:p.age,gender:p.gender,phone:p.phone,bp:p.bp,allergies:p.allergies,notes:cleanPatientNotes(p.notes),...profile,vitals:latest?{...latest,abnormal:abnormalVitals(latest)}:null,clinicalNotes:notes,investigationOrders:orders,labOrders:labOrders.filter(l=>l.patientId===p.id).map(l=>({id:l.id,testName:l.testName,category:l.category,status:l.status,result:l.result,notes:l.notes,orderedAt:l.orderedAt.toISOString(),resultedAt:l.resultedAt?.toISOString()||null})),diagnosticOrders:diagnosticOrders.filter(d=>d.patientId===p.id).map(d=>({id:d.id,studyName:d.studyName,modality:d.modality,bodyPart:d.bodyPart,status:d.status,findings:d.findings,impression:d.impression,notes:d.notes,orderedAt:d.orderedAt.toISOString(),reportedAt:d.reportedAt?.toISOString()||null})),encounterSummaries:encounters.filter(e=>e.patientId===p.id).map(e=>({id:e.id,date:e.date,chiefComplaint:e.chiefComplaint,diagnosis:e.diagnosis,assessment:e.assessment,plan:e.plan,clinicalNotes:e.clinicalNotes,createdAt:e.createdAt.toISOString()})),prescriptionSummaries:prescriptions.filter(r=>r.patientId===p.id).map(r=>({id:r.id,medicines:r.medicines,advice:r.advice,createdAt:r.createdAt.toISOString()})),currentHandover:handoverMap.get(p.id)||null}});const rooms=[...roomsMap.values()].map(r=>({...r,occupied:ipd.some(p=>p.roomNumber===r.roomNumber),patientName:ipd.find(p=>p.roomNumber===r.roomNumber)?.name||null}));const currentMembership=clinicId?await prisma.clinicMember.findFirst({where:{clinicId,doctorId:session.doctorId,isActive:true},select:{role:true,department:true,designation:true}}):null;const handoverRoles=new Set(["Owner","Admin","Manager","Consultant","Doctor","RMO","Nurse"]);const members=clinicId?await prisma.clinicMember.findMany({where:{clinicId,isActive:true,doctor:{isActive:true}},select:{id:true,doctorId:true,role:true,staffCode:true,designation:true,department:true,doctor:{select:{name:true}}},orderBy:[{department:"asc"},{doctor:{name:"asc"}}]}):[];const handoverOptions=members.filter(m=>handoverRoles.has(normalizeClinicRole(m.role))).map(m=>({id:m.id,doctorId:m.doctorId,name:m.doctor.name,role:normalizeClinicRole(m.role),staffCode:m.staffCode,designation:m.designation,department:m.department}));const ipdHistory=patients.filter(p=>parseCareSetting(p.notes)==="IPD"&&p.status!=="ACTIVE").map(p=>{const profile=parsePatientProfile(p.notes),vs=vitalsMap.get(p.id)||[],latest=vs[0],notes=(notesMap.get(p.id)||[]).sort((a,b)=>new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime()),orders=labOrders.filter(l=>l.patientId===p.id).sort((a,b)=>new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime());return{id:p.id,name:p.name,age:p.age,gender:p.gender,phone:p.phone,bp:p.bp,allergies:p.allergies,status:p.status,deletedAt:p.deletedAt,notes:cleanPatientNotes(p.notes),...profile,vitals:latest?{...latest,abnormal:abnormalVitals(latest)}:null,clinicalNotes:notes,investigationOrders:orders,labOrders:labOrders.filter(l=>l.patientId===p.id).map(l=>({id:l.id,testName:l.testName,category:l.category,status:l.status,result:l.result,notes:l.notes,orderedAt:l.orderedAt.toISOString(),resultedAt:l.resultedAt?.toISOString()||null})),diagnosticOrders:diagnosticOrders.filter(d=>d.patientId===p.id).map(d=>({id:d.id,studyName:d.studyName,modality:d.modality,bodyPart:d.bodyPart,status:d.status,findings:d.findings,impression:d.impression,notes:d.notes,orderedAt:d.orderedAt.toISOString(),reportedAt:d.reportedAt?.toISOString()||null})),encounterSummaries:encounters.filter(e=>e.patientId===p.id).map(e=>({id:e.id,date:e.date,chiefComplaint:e.chiefComplaint,diagnosis:e.diagnosis,assessment:e.assessment,plan:e.plan,clinicalNotes:e.clinicalNotes,createdAt:e.createdAt.toISOString()})),prescriptionSummaries:prescriptions.filter(r=>r.patientId===p.id).map(r=>({id:r.id,medicines:r.medicines,advice:r.advice,createdAt:r.createdAt.toISOString()})),}});return NextResponse.json({patients:ipd,ipdHistory,rooms,handoverOptions,currentRole:currentMembership?normalizeClinicRole(currentMembership.role):null,currentDepartment:currentMembership?.department||currentMembership?.designation||null},{headers:{"Cache-Control":"no-store"}});}
export async function POST(req:Request){const session=await getSession();if(!session)return NextResponse.json({error:"Unauthorized"},{status:401});const access=await requireClinicalModule(session.doctorId,"IPD");if(!access.allowed)return NextResponse.json({success:false,error:"IPD access is not included in this clinic's subscription."},{status:403});try{const body=await req.json(),action=String(body.action||""),clinicId=access.clinicId; const membership=await requireActiveClinicMembership(session.doctorId); const actorRole=membership?.role||"Clinician";if(action==="handover"){
  const patientId=String(body.patientId||"").trim();
  const receivingMemberId=String(body.receivingMemberId||"").trim();
  const context=String(body.context||"").trim();
  if(!patientId||!receivingMemberId||!context)return NextResponse.json({success:false,error:"Patient, receiving clinician/team and handover context are required."},{status:400});
  const eligibleRoles=new Set(["Owner","Admin","Manager","Consultant","Doctor","RMO","Nurse"]);
  try{
    const result=await prisma.$transaction(async tx=>{
      const patient=await tx.patient.findFirst({where:clinicId?{id:patientId,clinicId}:{id:patientId,doctorId:session.doctorId}});
      if(!patient)return {status:404,body:{success:false,error:"Patient not found in this clinic."}};
      if(patient.status!=="ACTIVE"||parseCareSetting(patient.notes)!=="IPD")return {status:409,body:{success:false,error:"Only an active IPD patient can be handed over."}};
      const receiving=await tx.clinicMember.findFirst({where:{id:receivingMemberId,clinicId,isActive:true,doctor:{isActive:true}},select:{id:true,doctorId:true,role:true,staffCode:true,designation:true,department:true,doctor:{select:{name:true}}}});
      if(!receiving)return {status:404,body:{success:false,error:"Receiving clinician/team is not an active member of this clinic."}};
      const receivingRole=normalizeClinicRole(receiving.role);
      if(!eligibleRoles.has(receivingRole))return {status:403,body:{success:false,error:"Selected receiving identity is not eligible for IPD clinical handover."}};
      if(receiving.doctorId===session.doctorId)return {status:409,body:{success:false,error:"Receiving clinician must be different from the acting clinician."}};
      await tx.patient.update({where:{id:patientId},data:{updatedAt:new Date()}});
      const clinicDoctorIds=(await tx.clinicMember.findMany({where:{clinicId,isActive:true},select:{doctorId:true}})).map(m=>m.doctorId);
      const latest=await tx.auditLog.findFirst({where:{entity:"IPDHandover",entityId:patientId,doctorId:{in:clinicDoctorIds}},orderBy:{createdAt:"desc"}});
      const latestMeta=metaOf(latest);
      if(String(latestMeta.receivingDoctorId||"")===receiving.doctorId)return {status:409,body:{success:false,error:"This patient is already handed over to the selected receiving clinician/team."}};
      const actorMember=await tx.clinicMember.findFirst({where:{doctorId:session.doctorId,clinicId,isActive:true},select:{role:true,staffCode:true,designation:true,department:true}});
      const actorRole=actorMember?.designation||actorMember?.role||"Clinician";
      const actorTeam=actorMember?.department||actorMember?.designation||actorMember?.role||"Clinical Team";
      const receivingTeam=receiving.department||receiving.designation||receivingRole||"Clinical Team";
      const now=new Date();
      const audit=await tx.auditLog.create({data:{doctorId:session.doctorId,action:"HANDOVER",entity:"IPDHandover",entityId:patientId,meta:JSON.stringify({clinicId,patientId,actorDoctorId:session.doctorId,actorRole,actorTeam,receivingMemberId:receiving.id,receivingDoctorId:receiving.doctorId,receivingName:receiving.doctor.name,receivingRole,receivingStaffCode:receiving.staffCode,receivingDesignation:receiving.designation,receivingTeam,context,at:now.toISOString(),atIst:istIsoLabel(now)})}});
      return {status:200,body:{success:true,patientId,auditId:audit.id,receiving:{memberId:receiving.id,doctorId:receiving.doctorId,name:receiving.doctor.name,role:receivingRole,team:receivingTeam}}};
    },{isolationLevel:"Serializable"});
    return NextResponse.json(result.body,{status:result.status});
  }catch(e:any){if(e?.code==="P2034")return NextResponse.json({success:false,error:"A concurrent handover was detected. Please refresh and try again."},{status:409});throw e;}
}
if(action==="set-department"){const patientId=String(body.patientId||"").trim();const department=String(body.department||"").trim();if(!patientId||!department)return NextResponse.json({success:false,error:"Patient and department are required"},{status:400});const patient=await getPatient(patientId,session.doctorId,clinicId);if(!patient)return NextResponse.json({success:false,error:"Patient not found"},{status:404});const profile=parsePatientProfile(patient.notes);const nextProfile={...profile,department};await prisma.patient.update({where:{id:patientId},data:{notes:encodePatientNotes(cleanPatientNotes(patient.notes),parseCareSetting(patient.notes),nextProfile)}});await writeAudit({doctorId:session.doctorId,action:"update",entity:"Patient",entityId:patientId,meta:{department,clinicId}});return NextResponse.json({success:true,department});}if(action==="room"){const roomNumber=String(body.roomNumber||"").trim();if(!roomNumber)return NextResponse.json({success:false,error:"Room number required"},{status:400});const existingRooms=await prisma.auditLog.findMany({where:{doctorId:{in:(await prisma.clinicMember.findMany({where:{clinicId,isActive:true},select:{doctorId:true}})).map(x=>x.doctorId)},entity:"HospitalRoom"},orderBy:{createdAt:"desc"},take:3000});if(existingRooms.some(l=>{const m=metaOf(l);return String(m.clinicId||"")===String(clinicId||"")&&String(m.roomNumber||"").trim()===roomNumber;}))return NextResponse.json({success:false,error:"Room already exists in this clinic."},{status:409});await writeAudit({doctorId:session.doctorId,action:"create",entity:"HospitalRoom",entityId:crypto.randomUUID(),meta:{roomNumber,roomCategory:String(body.roomCategory||"General Ward"),unitType:String(body.unitType||"Ward"),clinicId}});return NextResponse.json({success:true});}if(action==="clinical-note"){
  const requestedNoteType=String(body.noteType||"");
  if(requestedNoteType.toLowerCase().startsWith("nursing") || requestedNoteType.toLowerCase().includes("fall risk") || requestedNoteType.toLowerCase().includes("pressure injury")){
    if(normalizeClinicRole(actorRole)!=="Nurse")return NextResponse.json({success:false,error:"Only an active Nurse can create nursing documentation."},{status:403});
  }
  const patientId=String(body.patientId||"");
  const content=String(body.content||"").trim();
  const noteType=String(body.noteType||"Consultant Note");
  const submit=body.submit===true;
  const suppliedVitals = body.vitals && typeof body.vitals === "object" ? body.vitals : null;
  const suppliedAllergy = String(body.allergy ?? "").trim();
  if(!patientId||!content)return NextResponse.json({success:false,error:"Patient and note content required"},{status:400});
  const patient=await getPatient(patientId,session.doctorId,clinicId);
  if(!patient)return NextResponse.json({success:false,error:"Patient not found"},{status:404});
  if(noteType==="Medication Indent"){
    const prescription=await prisma.prescription.create({data:{doctorId:session.doctorId,patientId,patientName:patient.name,medicines:content,advice:String(body.notes||"")}});
    await writeAudit({doctorId:session.doctorId,action:"create",entity:"ClinicalNote",entityId:patientId,meta:{noteType,content,authorRole:"Medication",status:"Pending",orderId:prescription.id,clinicId}});
    return NextResponse.json({success:true,order:prescription});
  }
  if (suppliedAllergy) {
    await prisma.patient.update({where:{id:patient.id},data:{allergies:suppliedAllergy}});
  }
  const note=await prisma.clinicalNote.create({data:{
    clinicId:clinicId!,
    patientId,
    authorDoctorId:session.doctorId,
    noteType,
    title:String(body.title||""),
    content,
    status:submit?"PENDING_VERIFICATION":"DRAFT",
    version:1,
    contentHash:hashClinicalNote(content,1),
    submittedAt:submit?new Date():null,
  }});
  if (suppliedVitals) {
    await writeAudit({
      doctorId:session.doctorId,
      action:"create",
      entity:"NursingVital",
      entityId:patientId,
      meta:{
        bp:String(suppliedVitals.bp||""),
        pulse:String(suppliedVitals.pulse||""),
        rr:String(suppliedVitals.rr||""),
        spo2:String(suppliedVitals.spo2||""),
        temperature:String(suppliedVitals.temperature||""),
        findings:"",
        authorRole:actorRole,
        clinicId,
        source:"IPD clinical assessment",
      },
    });
  }
  await writeAudit({doctorId:session.doctorId,action:submit?"SUBMIT_FOR_VERIFICATION":"create",entity:"ClinicalNote",entityId:note.id,meta:{patientId,noteType,content,authorRole:actorRole,status:note.status,clinicId}});
  return NextResponse.json({success:true,note});
}
if(action==="lab-order"){const patientId=String(body.patientId||""),testName=String(body.testName||"").trim();if(!patientId||!testName)return NextResponse.json({success:false,error:"Patient and investigation are required"},{status:400});const patient=await getPatient(patientId,session.doctorId,clinicId);if(!patient)return NextResponse.json({success:false,error:"Patient not found"},{status:404});const order=await prisma.labOrder.create({data:{doctorId:session.doctorId,patientId,patientName:patient.name,testName,category:String(body.category||"Laboratory"),notes:String(body.notes||"")}});await writeAudit({doctorId:session.doctorId,action:"create",entity:"LabOrder",entityId:order.id,meta:{patientId,testName,category:order.category,ipd:true,clinicId}});await writeAudit({doctorId:session.doctorId,action:"create",entity:"ClinicalNote",entityId:patientId,meta:{noteType:"Investigation Indent",content:testName,orderId:order.id,status:"Pending",clinicId}});return NextResponse.json({success:true,order});}if(action==="emergency-contact"){const patientId=String(body.patientId||""),contactName=String(body.contactName||"").trim(),contactPhone=String(body.contactPhone||"").trim();if(!patientId||!contactName||!contactPhone)return NextResponse.json({success:false,error:"Patient, contact name and phone are required"},{status:400});const patient=await getPatient(patientId,session.doctorId,clinicId);if(!patient)return NextResponse.json({success:false,error:"Patient not found"},{status:404});const profile=parsePatientProfile(patient.notes),nextProfile={...profile,emergencyContact:{name:contactName,relationship:String(body.relationship||""),phone:contactPhone,alternatePhone:String(body.alternatePhone||"")}};await prisma.patient.update({where:{id:patientId},data:{notes:encodePatientNotes(cleanPatientNotes(patient.notes),parseCareSetting(patient.notes),nextProfile)}});await writeAudit({doctorId:session.doctorId,action:"update",entity:"Patient",entityId:patientId,meta:{emergencyContactUpdated:true,clinicId}});return NextResponse.json({success:true});}if(action==="room-transfer"){
  const patientId=String(body.patientId||"");
  const roomNumber=String(body.roomNumber||"").trim();
  if(!patientId||!roomNumber)return NextResponse.json({success:false,error:"Patient and destination room are required"},{status:400});
  const result=await prisma.$transaction(async tx=>{
    const patient=await tx.patient.findFirst({where:clinicId?{id:patientId,clinicId}:{id:patientId,doctorId:session.doctorId}});
    if(!patient)return {status:404,body:{success:false,error:"Patient not found in this clinic."}};
    if(patient.status!=="ACTIVE"||parseCareSetting(patient.notes)!=="IPD")return {status:409,body:{success:false,error:"Only an active IPD patient can be assigned or transferred."}};
    const profile=parsePatientProfile(patient.notes);
    const currentRoom=String(profile.roomNumber||"").trim();
    if(currentRoom===roomNumber)return {status:409,body:{success:false,error:"Patient is already assigned to this room."}};
    const members=await tx.clinicMember.findMany({where:{clinicId,isActive:true},select:{doctorId:true}});
    const roomLogs=await tx.auditLog.findMany({where:{doctorId:{in:members.map(x=>x.doctorId)},entity:"HospitalRoom"},orderBy:{createdAt:"desc"},take:3000});
    const roomExists=roomLogs.some(l=>{const m=metaOf(l);return String(m.clinicId||"")===String(clinicId||"")&&String(m.roomNumber||"").trim()===roomNumber;});
    if(!roomExists)return {status:404,body:{success:false,error:"Destination room is not in the hospital directory."}};
    const occupied=await tx.patient.findMany({where:{clinicId:clinicId||undefined,status:"ACTIVE"},select:{id:true,notes:true}});
    const occupiedByOther=occupied.some(p=>p.id!==patientId&&parseCareSetting(p.notes)==="IPD"&&String(parsePatientProfile(p.notes).roomNumber||"").trim()===roomNumber);
    if(occupiedByOther)return {status:409,body:{success:false,error:"Destination room is already occupied by another active IPD patient."}};
    const nextProfile={...profile,roomNumber};
    await tx.patient.update({where:{id:patientId},data:{notes:encodePatientNotes(cleanPatientNotes(patient.notes),parseCareSetting(patient.notes),nextProfile)}});
    const audit=await tx.auditLog.create({data:{doctorId:session.doctorId,action:"transfer",entity:"HospitalRoom",entityId:patientId,meta:JSON.stringify({clinicId,patientId,fromRoom:currentRoom||null,toRoom:roomNumber,actorRole})}});
    return {status:200,body:{success:true,patientId,fromRoom:currentRoom||null,toRoom:roomNumber,auditId:audit.id}};
  },{isolationLevel:"Serializable"});
  return NextResponse.json(result.body,{status:result.status});
}
if(action==="register"){const name=String(body.name||"").trim(),phone=String(body.phone||"").trim();if(!name||!phone)return NextResponse.json({success:false,error:"Name and phone required"},{status:400});const careSetting=body.careSetting==="IPD"?"IPD":"OPD";if(careSetting!=="IPD")return NextResponse.json({success:false,error:"The IPD endpoint only accepts IPD registrations."},{status:400});const profile={careSetting,address:String(body.address||""),idType:String(body.idType||""),idNumber:String(body.idNumber||""),mlcNumber:String(body.mlcNumber||""),prdNumber:String(body.prdNumber||""),wardType:String(body.wardType||""),unitType:String(body.unitType||""),roomNumber:String(body.roomNumber||""),admissionDate:body.admissionDate?String(body.admissionDate):new Date().toISOString(),chiefComplaint:String(body.chiefComplaint||""),hpi:String(body.hpi||""),pastHistory:String(body.pastHistory||""),surgicalHistory:String(body.surgicalHistory||""),systemicExam:String(body.systemicExam||""),workingDiagnosis:String(body.workingDiagnosis||""),diagnosis:String(body.diagnosis||""),icdCode:String(body.icdCode||""),consultantName:String(body.consultantName||""),consultantSpecialty:String(body.consultantSpecialty||"")};const patient=await prisma.patient.create({data:{doctorId:session.doctorId,clinicId,name,age:parseInt(body.age,10)||0,gender:String(body.gender||"Male"),phone,bp:"",allergies:String(body.allergies||""),notes:encodePatientNotes(String(body.notes||""),careSetting,profile)}});await writeAudit({doctorId:session.doctorId,action:"create",entity:"Patient",entityId:patient.id,meta:{careSetting,ipdRegistration:true,mlcNumber:profile.mlcNumber,prdNumber:profile.prdNumber,clinicId}});return NextResponse.json({success:true,patient:{id:patient.id}});}if(action==="vitals"){if(normalizeClinicRole(actorRole)!=="Nurse")return NextResponse.json({success:false,error:"Only an active Nurse can create or update nursing rounds."},{status:403});const patientId=String(body.patientId||"");if(!patientId)return NextResponse.json({success:false,error:"Patient required"},{status:400});const patient=await getPatient(patientId,session.doctorId,clinicId);if(!patient)return NextResponse.json({success:false,error:"Patient not found"},{status:404});const findings=String(body.findings||body.assessment||"").trim();await writeAudit({doctorId:session.doctorId,action:"create",entity:"NursingVital",entityId:patientId,meta:{bp:String(body.bp||""),pulse:String(body.pulse||""),rr:String(body.rr||""),spo2:String(body.spo2||""),temperature:String(body.temperature||""),findings,authorRole:"Nurse",clinicId}});if(findings)await writeAudit({doctorId:session.doctorId,action:"create",entity:"ClinicalNote",entityId:patientId,meta:{noteType:"Nursing Assessment",content:findings,authorRole:"Nurse",status:"Open",clinicId}});return NextResponse.json({success:true});}return NextResponse.json({success:false,error:"Unknown action"},{status:400});}catch(e){console.error("ipd api",e);return NextResponse.json({success:false,error:"Server error"},{status:500});}}
