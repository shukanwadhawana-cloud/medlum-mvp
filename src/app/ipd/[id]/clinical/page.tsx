"use client";

// The parent IPD page contains the full clinical workspace. This route gives
// that workspace its own page so the patient/admission landing screen stays compact.
export { default } from "../../page";
