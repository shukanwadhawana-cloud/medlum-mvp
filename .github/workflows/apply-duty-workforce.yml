name: Apply Duty Workforce (noop trigger)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Duty workforce schema is applied via restore-schema + migrate-deploy"
