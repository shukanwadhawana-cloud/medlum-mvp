/** MedLum API client — cookies + PostgreSQL */

export type ApiMembership = { clinicId: string; clinicName: string; role: "Owner" | "Admin" | "Consultant" | "Staff"; staffCode?: string; designation?: string; department?: string };
export type ApiDoctor = { id: string; name: string; email: string; clinicName: string; phone: string; createdAt: string; memberships?: ApiMembership[]; primaryRole?: ApiMembership["role"] | string; staffCode?: string; designation?: string; department?: string; isOwner?: boolean; hasWorkforceAdmin?: boolean };
const opts: RequestInit = { credentials: "include", headers: { "Content-Type": "application/json", "X-MedLum-Requested-With": "MedLum" } };
async function json<T>(res: Response): Promise<T> { return (await res.json().catch(() => ({}))) as T; }
async function confirmMutation(action: string, details?: string) { if (typeof window === "undefined") return true; return window.confirm("Confirm " + action + (details ? "\n\n" + details : "") + "\n\nOK = confirm. Cancel = leave unchanged."); }

export async function apiSignup(data: { name: string; email: string; password: string; clinicName: string; phone: string; facilityType: "HOSPITAL" | "CLINIC"; subscriptionModel: "OPD" | "IPD" | "BOTH"; licenseNumber: string; registrationNumber: string; ownerName: string; doctorInCharge: string; address: string; city: string; state: string; pincode: string }) { if (!await confirmMutation("facility/account creation", data.clinicName)) return { success:false, error:"Cancelled" }; const res = await fetch("/api/auth/signup", { ...opts, method: "POST", body: JSON.stringify(data) }); return json<{ success: boolean; error?: string; doctor?: ApiDoctor; productAccess?: { subscriptionModel: string } }>(res); }
export async function apiLogin(email: string, password: string) { const res = await fetch("/api/auth/login", { ...opts, method: "POST", body: JSON.stringify({ email, password }) }); return json<{ success: boolean; error?: string; doctor?: ApiDoctor; isOwner?: boolean; requiresOtp?: boolean; challengeId?: string; expiresAt?: string; deliveryChannel?: "console" | "telegram"; requiresTelegramLink?: boolean }>(res); }
export async function apiVerifyOtp(data: { doctorId: string; challengeId: string; code: string }) { const res = await fetch("/api/auth/otp/verify", { ...opts, method: "POST", body: JSON.stringify(data) }); return json<{ success: boolean; error?: string; doctor?: ApiDoctor }>(res); }
export async function apiLogout() { await fetch("/api/auth/logout", { ...opts, method: "POST" }); }
export async function apiGetMe() { try { const res = await fetch("/api/auth/me", { credentials: "include" }); if (!res.ok) return { success: false as const, doctor: null, isOwner: false }; return json<{ success: boolean; doctor: ApiDoctor | null; isOwner?: boolean }>(res); } catch { return { success: false as const, doctor: null, isOwner: false }; } }
export async function apiGetPatients() { try { const res = await fetch("/api/patients", { credentials: "include" }); if (!res.ok) return []; return (await json<{ patients: any[] }>(res)).patients || []; } catch { return []; } }
export async function apiAddPatient(data: any) { if (!await confirmMutation("patient creation", data?.name ? "Patient: "+data.name : undefined)) return { success: false, error: "Cancelled" }; const res = await fetch("/api/patients", { ...opts, method: "POST", body: JSON.stringify(data) }); return json<{ success: boolean; error?: string; patient?: any }>(res); }
export async function apiGetAppointments() { try { const res = await fetch("/api/appointments", { credentials: "include" }); if (!res.ok) return []; return (await json<{ appointments: any[] }>(res)).appointments || []; } catch { return []; } }
export async function apiAddAppointment(data: any) { if (!await confirmMutation("appointment", data?.date ? data.date+" "+(data.time||"") : undefined)) return { success: false, error: "Cancelled" }; const res = await fetch("/api/appointments", { ...opts, method: "POST", body: JSON.stringify(data) }); return json<{ success: boolean; error?: string; appointment?: any }>(res); }
export async function apiUpdateAppointmentStatus(id: string, status: string) { if (!await confirmMutation("appointment status change", "New status: "+status)) return { success: false, error: "Cancelled" }; const res = await fetch("/api/appointments", { ...opts, method: "PATCH", body: JSON.stringify({ id, status }) }); return json<{ success: boolean; error?: string; appointment?: any }>(res); }
export async function apiGetPrescriptions() { try { const res = await fetch("/api/prescriptions", { credentials: "include" }); if (!res.ok) return []; return (await json<{ prescriptions: any[] }>(res)).prescriptions || []; } catch { return []; } }
export async function apiAddPrescription(data: any) { if (!await confirmMutation("medication order", data?.medicines ? "Medicines: "+data.medicines : undefined)) return { success: false, error: "Cancelled" }; const res = await fetch("/api/prescriptions", { ...opts, method: "POST", body: JSON.stringify(data) }); return json<{ success: boolean; error?: string }>(res); }
