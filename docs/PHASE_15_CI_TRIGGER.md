# Phase 15 CI trigger

This marker commit intentionally forces a fresh GitHub Actions run against the corrected Phase 15 tree.

The canonical Render deployment is documented in `docs/DEPLOYMENT_RENDER.md`, and the pilot-readiness verifier must validate that documentation rather than weakening the check.
